macros:
  - name: get_validation_full
    description: |
      Execute the Audit Query returned by `audit_helper.compare_row_counts`, and
      log the result into the validation log table if `summarize=true`

macros:
  - name: get_validation_count
    description: |
      Execute the Audit Query returned by `audit_helper.compare_relations`, and
      log the result into the validation log table if `summarize=true`
